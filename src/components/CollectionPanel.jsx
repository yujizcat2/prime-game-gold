import { ATTRIBUTE_NAMES, ATTRIBUTES } from '../game/constants.js';
export default function CollectionPanel({ collection, onClose }) {
  return <div className="collection-overlay" onClick={onClose} role="presentation"><section className="collection-dialog" onClick={(event) => event.stopPropagation()} role="dialog" aria-modal="true" aria-label="收藏册"><header><div><small>COLLECTION</small><h2>收藏 {collection.size}/400</h2></div><button onClick={onClose} aria-label="关闭">×</button></header><div className="collection-grid"><b>数字</b>{ATTRIBUTES.map((attribute) => <b key={attribute}>{ATTRIBUTE_NAMES[attribute]}</b>)}{Array.from({ length: 100 }, (_, index) => index + 2).map((value) => <div className="collection-row" key={value}><strong>{value}</strong>{ATTRIBUTES.map((attribute) => <i className={collection.has(`${attribute}${value}`) ? `is-owned attr-${attribute}` : ''} key={attribute}>{collection.has(`${attribute}${value}`) ? '●' : '○'}</i>)}</div>)}</div></section></div>;
}
